# root:
# default/

# curl 127.0.0.1:8181

# Nginx behavior: return 403 error

# Webserv result: OK as webserv force to have the index directive file within the root folder

server {
    listen       8181;
    host         127.0.0.1;
    index        index.html;
    root         default;
}