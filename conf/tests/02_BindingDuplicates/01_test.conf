# 2 same sockets, what happens when binding with no control before?

server {
  listen 6578;
  host 127.0.0.1;
  server_name localhost;
  root root/test/;
  index index.html;
}

server {
  listen 6578;
  host 127.0.0.1;
  server_name localhost;
  root root/test/;
  index index.html;
}